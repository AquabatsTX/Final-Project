# FINAL
This ArcGIS Pro script tool converts a tabular dataset with X/Y or latitude/longitude coordinate fields into spatial point features, classifies each point by Edwards Aquifer zone, checks whether each point falls within Hays County, and calculates the distance from each point to the nearest Hays County stream.
